{
  "name": "myweb",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "deploy": "npm run build && npm --prefix deploy run deploy:files",
    "deploy:files": "npm --prefix deploy run deploy:files"
  }
}
